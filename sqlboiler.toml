output   = "main/models/generated"
wipe     = true
no-tests = true

# Convert to just using env variables: psql_dbname
[psql]
dbname = "coinprice"
host   = "localhost"
port   = 5432
user   = "postgres"
pass   = "postgres"
sslmode = "disable"
